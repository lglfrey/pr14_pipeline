name: Node.js CI

on: [push, pull_request]

jobs:
  check-codestyle:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ['14.x', '16.x']  # Пример использования матрицы для нескольких версий Node.js

    steps:
    - uses: actions/checkout@v2  # Обновлено до последней версии

    - name: Setup Node.js
      uses: actions/setup-node@v3  # Обновлено до последней версии
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install dependencies
      run: npm install

    - name: Check code style
      run: npm run codestyle
      env:
        CI: true
